# 磨平 App 开发执行包 v0.1

## 0. 文档目的

本文档用于把当前已经确认的产品方向和原型，压缩成一份可直接进入工程开发的执行包。

适用对象：
- iOS 开发
- Apple Watch 开发
- 后端/API 开发
- AI/算法接入
- Codex 或其他代码代理继续实现

当前版本目标：
**先完成最小闭环 MVP：拍食物 → 估算热量 → 生成磨平任务 → 派发到 Apple Watch → 运动完成后结算 → 生成分享卡。**

---

# 1. 产品定义

## 1.1 产品一句话

**拍一下你要吃的东西，看看你得动多久。**

## 1.2 产品定位

不是传统卡路里记录工具，而是一个：

**把食物热量转成 Apple Watch 运动任务，并通过“磨平/结清”机制提升行动意愿的游戏化健康产品。**

## 1.3 MVP 核心价值

用户不是“记一顿饭”，而是：
1. 拍下食物
2. 看到这口东西大概要花多少运动来磨平
3. 选择一种还法
4. 在 Apple Watch 上开始任务
5. 完成后得到“已磨平 / 已结清”的反馈
6. 可选择生成战绩卡分享

---

# 2. MVP 范围冻结

## 2.1 本期必须做

### iPhone 端
- 首页
- 拍照入口
- 食物分析结果页
- 任务选择页
- Apple Watch 派发页
- 任务完成结果页
- 分享卡页

### Apple Watch 端
- 接收任务
- 展示当前任务卡
- 启动运动
- 同步运动进度
- 回传进度/完成状态
- 展示“已结清”状态

### 基础能力
- 食物热量估算（可先用近似逻辑）
- 热量 → 运动时长换算
- 任务状态管理
- HealthKit / Workout 数据读取
- 分享卡生成

## 2.2 本期不做
- 社区系统完整实现
- 评论/好友/关注关系
- 复杂排行榜
- 多食物合并任务系统
- 多任务嵌套拆单
- 深度营养报告
- 三餐全覆盖精细识别
- 临床健康建议
- 复杂会员体系

---

# 3. 用户路径

## 3.1 主路径

1. 用户打开 iPhone App 首页
2. 点击“拍一下，接一单”
3. 拍摄食物照片
4. App 返回：
   - 食物名称（可估）
   - 热量估算
   - 红/橙/黄/绿任务等级
   - 推荐运动时长
5. 用户选择任务模式：
   - 轻松磨平
   - 标准磨平
   - 高效磨平
6. 用户点击“派发到 Apple Watch”
7. Apple Watch 收到任务卡
8. 用户在 Apple Watch 上启动运动
9. HealthKit / WorkoutKit / WorkoutSession 记录运动
10. 任务进度回传 iPhone
11. 当达到设定消耗目标后，任务变为“已结清”
12. App 生成分享卡

## 3.2 次路径

### 未完成任务
- 用户运动中断
- 任务保留在“待磨平”列表
- 下次可继续完成

### 部分完成
- 例如只完成 42%
- UI 显示“已磨平 42%”
- 不强制一次结清

---

# 4. 页面清单

## 4.1 iPhone 页面

### P1 首页
目的：进入主动作。

模块：
- 今日待磨平 kcal
- 今日已结清单数
- 连续天数
- 主 CTA：拍一下，接一单
- 可选：最近战绩入口

核心按钮：
- 拍照
- 查看今日账单

### P2 拍照页
目的：采集食物图片。

模块：
- 相机预览
- 拍照按钮
- 取消按钮

首版要求：
- 只支持单张图
- 暂不做复杂拍照引导

### P3 食物分析页
目的：告诉用户“这口值你动多久”。

展示字段：
- 图片
- 食物名称
- 热量估算 kcal
- 任务等级
- 风险标签（可简化）
- 推荐运动时长
- 文案提示

按钮：
- 生成磨平任务

### P4 任务选择页
目的：把同一热量转成不同“还法”。

任务模式：
- 轻松磨平：快走
- 标准磨平：跑走结合
- 高效磨平：跑步/跳绳

展示字段：
- 模式名
- 描述
- 预计时长
- 是否支持分段完成

按钮：
- 派发到 Apple Watch

### P5 任务同步页
目的：显示任务已下发和同步状态。

展示字段：
- 当前任务名称
- 当前目标 kcal
- 当前目标分钟数
- 当前同步状态
- 当前完成进度

按钮：
- 查看手表任务
- 返回首页

### P6 完成结果页
目的：给用户即时成就反馈。

状态：
- 进行中
- 已磨平 xx%
- 已结清

展示字段：
- 已完成百分比
- 已消耗 kcal
- 剩余 kcal
- 本单状态

按钮：
- 生成分享卡
- 返回首页

### P7 分享卡页
目的：生成可传播战绩图。

字段：
- 食物名称
- 今日结清数
- 本单状态
- 连续磨平天数
- 一句文案

按钮：
- 分享
- 保存图片

---

## 4.2 Apple Watch 页面

### W1 任务列表/任务卡
展示字段：
- 食物名称
- 目标 kcal
- 推荐方式
- 目标分钟数
- 进度

按钮：
- 开始
- 稍后继续

### W2 运动中页
展示字段：
- 当前任务名
- 已磨平百分比
- 已消耗 kcal
- 剩余 kcal
- 剩余分钟数（估算）

按钮：
- 暂停
- 结束

### W3 完成页
展示字段：
- 已结清
- 本次完成 kcal
- 完成时间

按钮：
- 完成

---

# 5. 数据模型

## 5.1 FoodTask

```ts
interface FoodTask {
  id: string;
  foodName: string;
  foodImageUrl?: string;
  estimatedCalories: number;
  taskLevel: "green" | "yellow" | "orange" | "red";
  taskMode: "walk" | "mix" | "run";
  targetBurnCalories: number;
  estimatedMinutes: number;
  status: "created" | "synced" | "in_progress" | "paused" | "completed" | "cancelled";
  progressPercent: number;
  burnedCalories: number;
  createdAt: string;
  completedAt?: string;
}
```

## 5.2 WorkoutProgress

```ts
interface WorkoutProgress {
  taskId: string;
  activeEnergyBurned: number;
  durationSeconds: number;
  source: "apple_watch" | "iphone";
  lastSyncAt: string;
}
```

## 5.3 ShareCard

```ts
interface ShareCard {
  taskId: string;
  title: string;
  subtitle: string;
  progressPercent: number;
  streakDays: number;
  clearedCountToday: number;
  imageUrl?: string;
}
```

---

# 6. 状态机定义

## 6.1 任务状态

```text
created
  -> synced
  -> in_progress
  -> paused
  -> in_progress
  -> completed
```

补充分支：
- created -> cancelled
- synced -> cancelled
- paused -> cancelled

## 6.2 结清规则

当 `burnedCalories >= targetBurnCalories` 时：
- status = completed
- progressPercent = 100
- 生成“已结清”结果
- 解锁分享卡

## 6.3 部分完成规则

当 `0 < burnedCalories < targetBurnCalories` 时：
- status = in_progress 或 paused
- progressPercent = floor(burnedCalories / targetBurnCalories * 100)
- 分享卡可选解锁“已磨平 xx%”版本

---

# 7. 热量换算与任务生成规则

## 7.1 输入
- 食物图片
- 食物识别名称（可近似）
- 热量估算 kcal

## 7.2 输出
- 任务等级
- 三种任务模式的建议分钟数

## 7.3 MVP 近似规则

### 任务等级
- green: < 120 kcal
- yellow: 120 - 219 kcal
- orange: 220 - 349 kcal
- red: >= 350 kcal

### 换算系数（首版可配置）
假设：
- 快走：8.7 kcal/分钟
- 跑走结合：13 kcal/分钟
- 跑步：18.5 kcal/分钟

### 生成分钟数
```ts
walkMinutes = ceil(kcal / 8.7)
mixMinutes = ceil(kcal / 13)
runMinutes = ceil(kcal / 18.5)
```

注意：
首版只求闭环，不追求运动学绝对精确。
后续可按用户体重、性别、年龄个性化修正。

---

# 8. 食物识别策略

## 8.1 首版策略

不追求复杂通用识别模型。

建议首版采用：
- 零食/奶茶/甜品/饮料为主
- 可通过图像识别 + 文本归类 + 预置热量库近似实现

## 8.2 首期优先支持品类
- 奶茶
- 碳酸饮料
- 袋装零食
- 冰淇淋
- 蛋糕/甜甜圈
- 炸鸡/薯条

## 8.3 推荐实现方式
MVP 可先采用：
- 图像大模型识别食物类别
- 后端将类别映射到本地热量估算表

例如：
- 黑糖珍珠奶茶 → 340 kcal
- 大包薯片 → 280 kcal
- 甜甜圈 → 260 kcal

---

# 9. API 草案

## 9.1 POST /analyze-food

输入：
```json
{
  "imageBase64": "..."
}
```

输出：
```json
{
  "foodName": "黑糖珍珠奶茶",
  "estimatedCalories": 340,
  "taskLevel": "red",
  "suggestions": {
    "walk": 39,
    "mix": 26,
    "run": 18
  },
  "tags": ["高糖", "液体热量"]
}
```

## 9.2 POST /tasks

输入：
```json
{
  "foodName": "黑糖珍珠奶茶",
  "estimatedCalories": 340,
  "taskMode": "mix",
  "targetBurnCalories": 340,
  "estimatedMinutes": 26
}
```

输出：
```json
{
  "taskId": "task_123",
  "status": "created"
}
```

## 9.3 POST /tasks/{taskId}/sync-watch

作用：
- 标记任务已派发
- 同步手表侧任务信息

输出：
```json
{
  "taskId": "task_123",
  "status": "synced"
}
```

## 9.4 POST /tasks/{taskId}/progress

输入：
```json
{
  "activeEnergyBurned": 126,
  "durationSeconds": 720,
  "source": "apple_watch"
}
```

输出：
```json
{
  "taskId": "task_123",
  "status": "in_progress",
  "progressPercent": 37,
  "burnedCalories": 126,
  "remainingCalories": 214
}
```

## 9.5 POST /tasks/{taskId}/complete

输出：
```json
{
  "taskId": "task_123",
  "status": "completed",
  "progressPercent": 100,
  "shareCardUnlocked": true
}
```

---

# 10. iOS 技术建议

## 10.1 iPhone 技术栈
- SwiftUI
- MVVM
- Async/Await
- HealthKit
- WatchConnectivity
- 可选：WorkoutKit 对接计划训练

## 10.2 Apple Watch 技术栈
- watchOS + SwiftUI
- HealthKit
- HKWorkoutSession
- HKLiveWorkoutBuilder
- WatchConnectivity

## 10.3 状态同步建议
- iPhone 负责任务创建和管理
- Apple Watch 负责实时运动数据采集
- 双端通过 WatchConnectivity 同步任务状态
- 同时把关键状态写回服务端，便于任务恢复

---

# 11. HealthKit / Apple Watch 实现建议

## 11.1 关键能力
需要实现：
- 请求 HealthKit 权限
- 读取 activeEnergyBurned
- 创建 workout session
- 监听 workout 进行中的能量消耗
- 结束时回写任务完成状态

## 11.2 MVP 推荐方案
首版先不要做过多运动类型。
优先支持：
- Walking
- Running
- Cycling（可第二阶段）

建议先把 Walking / Running 做稳。

---

# 12. 分享卡规则

## 12.1 分享卡类型

### A. 已结清卡
文案示例：
- 已结清：黑糖珍珠奶茶
- 这口我没有赖账
- 今日结清 1 单

### B. 部分完成卡
文案示例：
- 已磨平 42%
- 先还一半也算赢
- 今天先把它拉回来一点

## 12.2 生成方式
可先使用前端本地渲染导出图片。
首版无需服务端生成。

---

# 13. 埋点建议

关键埋点：
- 首页曝光
- 点击拍照
- 拍照完成
- 食物识别成功
- 任务页曝光
- 点击派发到 Watch
- Watch 任务开始
- Watch 任务完成
- 分享卡生成
- 分享按钮点击
- 部分完成中断

---

# 14. 开发阶段拆解

## Phase 1：原型工程化
目标：把现有原型做成真实可运行 App 壳子

任务：
- 建立 iOS SwiftUI 项目
- 建立 watchOS Target
- 搭建导航结构
- 还原主要页面
- 用 mock 数据跑通 UI

## Phase 2：任务闭环打通
目标：完成任务创建和状态流转

任务：
- 实现 FoodTask model
- 实现任务状态机
- 接入 mock analyze-food API
- 接入 mock sync-watch API
- 任务完成页打通

## Phase 3：HealthKit / Watch 接入
目标：使用真实运动数据驱动任务完成

任务：
- 请求权限
- 创建 workout session
- 读取 active energy
- 回传进度
- 自动结清

## Phase 4：分享卡与 polish
目标：完成 MVP 可演示版本

任务：
- 生成分享卡
- 文案 polish
- 动效
- 错误态
- loading 状态

---

# 15. 两周开发优先级建议

## 第 1 周
- SwiftUI 项目搭建
- 页面结构实现
- mock 数据驱动
- 任务状态机
- iPhone → Watch 任务同步骨架

## 第 2 周
- Watch workout session
- 实时进度更新
- 结果页
- 分享卡
- demo 可跑通

---

# 16. Codex 交接说明

下面内容可直接作为 Codex 的任务说明。

---

## 给 Codex 的任务 Prompt

你现在要继续实现一个名为“磨平”的 iOS + Apple Watch MVP。

### 产品目标
用户拍摄一张食物照片，系统估算热量，然后生成一个“磨平任务”。这个任务可以派发到 Apple Watch。用户完成运动后，通过 HealthKit/Workout 数据更新任务进度，达到目标后任务标记为 completed，并生成一张可分享的战绩卡。

### 当前设计方向
产品偏游戏化和社交传播感，但 MVP 不做完整社区系统。重点是跑通以下闭环：
- iPhone 拍照
- 食物分析（允许 mock）
- 选择任务模式（walk / mix / run）
- 派发到 Apple Watch
- Apple Watch 开始 workout
- 更新 active energy burned
- 回传任务进度
- 达到目标后完成任务
- 展示分享卡

### 请先完成的工程目标
1. 建立 SwiftUI iOS 工程
2. 建立 watchOS target
3. 实现以下页面：
   - HomeView
   - ScanView
   - AnalysisView
   - TaskModeView
   - WatchSyncView
   - ResultView
   - ShareCardView
4. 实现以下模型：
   - FoodTask
   - WorkoutProgress
   - ShareCard
5. 实现任务状态机：
   - created
   - synced
   - in_progress
   - paused
   - completed
6. 使用 mock API / local mock service 返回食物分析结果
7. 接入 WatchConnectivity 进行任务同步
8. 在 watchOS 侧实现简化版 workout session
9. 用 activeEnergyBurned 驱动进度条
10. 完成后展示“已结清”结果页

### 代码要求
- 使用 SwiftUI
- 使用 MVVM
- 尽量模块化
- 提供清晰的 mock service
- 每个页面和 view model 分文件组织
- 给出必要注释
- 先以 demo 可跑通为目标，不追求所有边界情况

### 建议目录结构
- App/
- Features/Home/
- Features/Scan/
- Features/Analysis/
- Features/Task/
- Features/Workout/
- Features/Share/
- Models/
- Services/
- WatchApp/

### 重要说明
- 食物识别可以先用 mock 数据，不要求真实 AI 模型
- 热量换算先用简单规则
- 先完成最短闭环，再做 polish

请先输出项目结构和第一批 SwiftUI 文件实现。

---

# 17. 工程文件结构建议

## 17.1 iOS App 目录结构

```text
Moping/
├── App/
│   ├── MopingApp.swift
│   ├── AppCoordinator.swift
│   ├── AppRoute.swift
│   └── RootTabView.swift
│
├── Core/
│   ├── DesignSystem/
│   │   ├── Colors+
│   │   ├── Typography+
│   │   ├── Spacing+
│   │   ├── Components/
│   │   │   ├── PrimaryButton.swift
│   │   │   ├── TaskLevelBadge.swift
│   │   │   ├── StatCard.swift
│   │   │   ├── ProgressRing.swift
│   │   │   └── SectionHeader.swift
│   ├── Extensions/
│   ├── Utils/
│   └── Constants/
│
├── Models/
│   ├── FoodTask.swift
│   ├── WorkoutProgress.swift
│   ├── ShareCard.swift
│   ├── FoodAnalysisResult.swift
│   ├── TaskMode.swift
│   ├── TaskLevel.swift
│   └── AppError.swift
│
├── Services/
│   ├── Protocols/
│   │   ├── FoodAnalysisServicing.swift
│   │   ├── TaskServicing.swift
│   │   ├── WorkoutServicing.swift
│   │   ├── ShareCardServicing.swift
│   │   └── WatchSyncServicing.swift
│   ├── Mock/
│   │   ├── MockFoodAnalysisService.swift
│   │   ├── MockTaskService.swift
│   │   ├── MockWorkoutService.swift
│   │   ├── MockShareCardService.swift
│   │   └── MockWatchSyncService.swift
│   ├── Live/
│   │   ├── HealthKitWorkoutService.swift
│   │   ├── WatchConnectivityService.swift
│   │   └── ShareCardRenderer.swift
│   └── DependencyContainer.swift
│
├── Repositories/
│   ├── TaskRepository.swift
│   └── LocalTaskStore.swift
│
├── Features/
│   ├── Home/
│   │   ├── HomeView.swift
│   │   ├── HomeViewModel.swift
│   │   ├── Components/
│   │   │   ├── TodayDebtCard.swift
│   │   │   ├── QuickActionCard.swift
│   │   │   └── TodayStatsGrid.swift
│   │   └── HomeCoordinator.swift
│   │
│   ├── Scan/
│   │   ├── ScanView.swift
│   │   ├── ScanViewModel.swift
│   │   ├── CameraPreviewView.swift
│   │   └── PhotoCaptureService.swift
│   │
│   ├── Analysis/
│   │   ├── AnalysisView.swift
│   │   ├── AnalysisViewModel.swift
│   │   └── Components/
│   │       ├── FoodHeroCard.swift
│   │       ├── CalorieMetricCard.swift
│   │       └── TaskHintPanel.swift
│   │
│   ├── TaskMode/
│   │   ├── TaskModeView.swift
│   │   ├── TaskModeViewModel.swift
│   │   └── Components/
│   │       ├── TaskModeOptionCard.swift
│   │       └── TaskSummaryCard.swift
│   │
│   ├── WatchSync/
│   │   ├── WatchSyncView.swift
│   │   ├── WatchSyncViewModel.swift
│   │   └── Components/
│   │       ├── SyncStatusBanner.swift
│   │       └── LiveProgressCard.swift
│   │
│   ├── Result/
│   │   ├── ResultView.swift
│   │   ├── ResultViewModel.swift
│   │   └── Components/
│   │       ├── CompletionHero.swift
│   │       ├── ProgressSummaryGrid.swift
│   │       └── ResultActionBar.swift
│   │
│   ├── Share/
│   │   ├── ShareCardView.swift
│   │   ├── ShareCardViewModel.swift
│   │   ├── ShareComposerView.swift
│   │   └── Components/
│   │       └── SharePreviewCard.swift
│   │
│   └── TaskList/
│       ├── TaskListView.swift
│       ├── TaskListViewModel.swift
│       └── Components/
│           └── TaskRow.swift
│
├── PreviewContent/
│   ├── PreviewMocks.swift
│   └── PreviewImages/
│
└── Resources/
    ├── Assets.xcassets
    └── MockData/
        ├── food_analysis_samples.json
        ├── share_card_templates.json
        └── demo_tasks.json
```

## 17.2 Apple Watch 目录结构

```text
MopingWatchApp/
├── App/
│   ├── MopingWatchApp.swift
│   └── WatchRootView.swift
│
├── Models/
│   ├── WatchTaskPayload.swift
│   └── WatchWorkoutState.swift
│
├── Services/
│   ├── WatchWorkoutManager.swift
│   ├── WatchSyncReceiver.swift
│   └── WatchHealthPermissionManager.swift
│
├── Features/
│   ├── TaskInbox/
│   │   ├── WatchTaskInboxView.swift
│   │   └── WatchTaskInboxViewModel.swift
│   ├── Workout/
│   │   ├── WatchWorkoutView.swift
│   │   ├── WatchWorkoutViewModel.swift
│   │   └── Components/
│   │       ├── WatchProgressRing.swift
│   │       └── WatchMetricStrip.swift
│   └── Completion/
│       ├── WatchCompletionView.swift
│       └── WatchCompletionViewModel.swift
└── Shared/
    └── WatchMessageTypes.swift
```

---

# 18. SwiftUI 页面命名与职责边界

## 18.1 页面命名建议

统一使用：
- `XxxView.swift`：页面本身
- `XxxViewModel.swift`：状态与业务逻辑
- `XxxCoordinator.swift`：需要时负责局部导航
- `XxxOptionCard.swift` / `XxxMetricCard.swift`：可复用组件

避免使用：
- `MainPage.swift`
- `Screen1.swift`
- `TempView.swift`
- `NewTaskPage.swift`

推荐命名如下：
- `HomeView`
- `ScanView`
- `AnalysisView`
- `TaskModeView`
- `WatchSyncView`
- `ResultView`
- `ShareCardView`
- `TaskListView`

## 18.2 各页面职责

### HomeView
只负责：
- 展示今日概览
- 展示主 CTA
- 导航到 ScanView 或 TaskListView

不负责：
- 直接处理拍照结果解析
- 直接管理 workout session

### ScanView
只负责：
- 获取图片
- 触发分析
- 跳转 AnalysisView

不负责：
- 任务生成逻辑
- 结果文案拼装

### AnalysisView
只负责：
- 展示分析结果
- 根据分析结果进入任务生成

不负责：
- 真正发起 Apple Watch 同步

### TaskModeView
只负责：
- 给用户选还法
- 构造 FoodTask
- 请求创建任务

### WatchSyncView
只负责：
- 调用 WatchSyncService
- 展示同步状态
- 展示实时进度

### ResultView
只负责：
- 读任务完成状态
- 展示“已磨平/已结清”
- 进入分享流程

### ShareCardView
只负责：
- 渲染分享卡
- 导出图片
- 调起系统分享

---

# 19. ViewModel 拆分建议

## 19.1 HomeViewModel

职责：
- 拉取今日任务摘要
- 计算今日待磨平 kcal
- 计算已结清任务数
- 计算 streak
- 导航意图输出

建议状态：
```ts
struct HomeViewState {
  let todayPendingCalories: Int
  let clearedCountToday: Int
  let streakDays: Int
  let latestTasks: [FoodTask]
  let isLoading: Bool
}
```

## 19.2 ScanViewModel

职责：
- 管理拍照权限
- 管理拍照结果
- 把图片传给 FoodAnalysisService
- 管理 loading / error

建议状态：
```ts
struct ScanViewState {
  let capturedImage: UIImage?
  let isAnalyzing: Bool
  let errorMessage: String?
}
```

## 19.3 AnalysisViewModel

职责：
- 接收 `FoodAnalysisResult`
- 组装展示文案
- 处理“生成磨平任务”按钮点击

建议状态：
```ts
struct AnalysisViewState {
  let result: FoodAnalysisResult
  let suggestedModes: [TaskModeSuggestion]
  let heroMessage: String
}
```

## 19.4 TaskModeViewModel

职责：
- 维护当前选中的任务模式
- 构建 `FoodTask`
- 调用 TaskService 创建任务

建议状态：
```ts
struct TaskModeViewState {
  let analysisResult: FoodAnalysisResult
  let selectedMode: TaskMode
  let allModes: [TaskModeSuggestion]
  let isCreatingTask: Bool
}
```

## 19.5 WatchSyncViewModel

职责：
- 派发任务到 Apple Watch
- 监听同步状态
- 监听进度更新
- 判断是否已完成

建议状态：
```ts
struct WatchSyncViewState {
  let task: FoodTask
  let syncStatus: WatchSyncStatus
  let progressPercent: Int
  let burnedCalories: Int
  let remainingCalories: Int
}
```

## 19.6 ResultViewModel

职责：
- 根据任务当前状态生成结果文案
- 判断是否解锁分享卡
- 处理返回首页/去分享

建议状态：
```ts
struct ResultViewState {
  let task: FoodTask
  let title: String
  let subtitle: String
  let progressPercent: Int
  let shareUnlocked: Bool
}
```

## 19.7 ShareCardViewModel

职责：
- 组装 ShareCard
- 控制导出图片
- 控制系统分享

建议状态：
```ts
struct ShareCardViewState {
  let card: ShareCard
  let isExporting: Bool
  let exportErrorMessage: String?
}
```

---

# 20. Mock 数据规范

## 20.1 目标

让 Codex 在没有真实 AI、没有真实后端、甚至没有真实 Apple Watch 的情况下，仍然能把演示版跑起来。

原则：
- 所有外部依赖先 mock
- mock 结构尽量贴近未来真实接口
- mock 数据要覆盖成功态、失败态、部分完成态

## 20.2 食物分析 mock

建议文件：`Resources/MockData/food_analysis_samples.json`

示例：
```json
[
  {
    "id": "sample_milktea",
    "foodName": "黑糖珍珠奶茶",
    "estimatedCalories": 340,
    "taskLevel": "red",
    "tags": ["高糖", "液体热量"],
    "suggestions": {
      "walk": 39,
      "mix": 26,
      "run": 18
    },
    "heroMessage": "这口代价不低，但很适合接一张单。"
  },
  {
    "id": "sample_chips",
    "foodName": "海盐薯片",
    "estimatedCalories": 280,
    "taskLevel": "orange",
    "tags": ["高油", "零食型热量"],
    "suggestions": {
      "walk": 33,
      "mix": 22,
      "run": 16
    },
    "heroMessage": "它不夸张，但会悄悄把今天的账抬高。"
  },
  {
    "id": "sample_donut",
    "foodName": "糖霜甜甜圈",
    "estimatedCalories": 260,
    "taskLevel": "orange",
    "tags": ["高糖", "精制碳水"],
    "suggestions": {
      "walk": 30,
      "mix": 20,
      "run": 15
    },
    "heroMessage": "不一定不能吃，但最好别假装没发生。"
  }
]
```

## 20.3 任务 mock

建议文件：`Resources/MockData/demo_tasks.json`

示例：
```json
[
  {
    "id": "task_001",
    "foodName": "黑糖珍珠奶茶",
    "estimatedCalories": 340,
    "taskLevel": "red",
    "taskMode": "mix",
    "targetBurnCalories": 340,
    "estimatedMinutes": 26,
    "status": "in_progress",
    "progressPercent": 42,
    "burnedCalories": 143,
    "createdAt": "2026-03-17T18:20:00Z"
  },
  {
    "id": "task_002",
    "foodName": "芝士蛋糕",
    "estimatedCalories": 310,
    "taskLevel": "orange",
    "taskMode": "walk",
    "targetBurnCalories": 310,
    "estimatedMinutes": 36,
    "status": "completed",
    "progressPercent": 100,
    "burnedCalories": 312,
    "createdAt": "2026-03-17T12:40:00Z",
    "completedAt": "2026-03-17T15:10:00Z"
  }
]
```

## 20.4 分享卡 mock

建议文件：`Resources/MockData/share_card_templates.json`

示例：
```json
[
  {
    "style": "completed",
    "title": "已结清",
    "subtitle": "这口我没有赖账",
    "footer": "今日结清 1 单"
  },
  {
    "style": "partial",
    "title": "已磨平 {{progress}}%",
    "subtitle": "先还一半也算赢",
    "footer": "今天先把它拉回来一点"
  }
]
```

## 20.5 进度模拟规则

MockWorkoutService 可采用定时器每 1 秒增加一次：
- `activeEnergyBurned += 8 ~ 15 kcal`
- `durationSeconds += 60`
- 自动计算 `progressPercent`

这样不用真实手表，也能在 iPhone 模拟器里演示整条链路。

---

# 21. 依赖注入与环境切换建议

## 21.1 DependencyContainer

建议统一在 `DependencyContainer` 中注册：
- `foodAnalysisService`
- `taskService`
- `workoutService`
- `shareCardService`
- `watchSyncService`

示例策略：
- Debug：默认 Mock
- Demo：Analysis Mock + Workout Mock
- Production：Live Service

## 21.2 Environment 开关

建议加一个简单配置：
```ts
enum AppEnvironment {
  case mock
  case demo
  case production
}
```

作用：
- mock：全本地
- demo：UI 真、部分数据真
- production：接正式服务

---

# 22. 首批 SwiftUI 文件实现优先级

## 22.1 第一批必须先建
1. `MopingApp.swift`
2. `AppCoordinator.swift`
3. `HomeView.swift`
4. `HomeViewModel.swift`
5. `ScanView.swift`
6. `ScanViewModel.swift`
7. `AnalysisView.swift`
8. `AnalysisViewModel.swift`
9. `TaskModeView.swift`
10. `TaskModeViewModel.swift`
11. `FoodTask.swift`
12. `FoodAnalysisResult.swift`
13. `MockFoodAnalysisService.swift`
14. `MockTaskService.swift`

## 22.2 第二批
1. `WatchSyncView.swift`
2. `WatchSyncViewModel.swift`
3. `ResultView.swift`
4. `ResultViewModel.swift`
5. `ShareCardView.swift`
6. `ShareCardViewModel.swift`
7. `MockWorkoutService.swift`
8. `MockShareCardService.swift`

## 22.3 第三批
1. watchOS target
2. `WatchTaskInboxView.swift`
3. `WatchWorkoutView.swift`
4. `WatchWorkoutManager.swift`
5. `WatchSyncReceiver.swift`
6. `HealthKitWorkoutService.swift`
7. `WatchConnectivityService.swift`

---

# 23. 给 Codex 的补充 Prompt

下面这段可以作为第二轮交接内容继续喂给 Codex。

---

## 给 Codex 的补充工程 Prompt

请基于现有“磨平”产品原型，继续按以下工程约束实现：

### 工程结构要求
- 使用 SwiftUI + MVVM
- 目录按 Features / Models / Services / Core / Resources 拆分
- 每个页面一个 View 文件、一个 ViewModel 文件
- 通过 DependencyContainer 注入 mock / live service
- 先默认运行在 mock 环境

### 请优先创建以下文件
- App/MopingApp.swift
- App/AppCoordinator.swift
- Models/FoodTask.swift
- Models/FoodAnalysisResult.swift
- Models/TaskMode.swift
- Services/Protocols/FoodAnalysisServicing.swift
- Services/Protocols/TaskServicing.swift
- Services/Mock/MockFoodAnalysisService.swift
- Services/Mock/MockTaskService.swift
- Features/Home/HomeView.swift
- Features/Home/HomeViewModel.swift
- Features/Scan/ScanView.swift
- Features/Scan/ScanViewModel.swift
- Features/Analysis/AnalysisView.swift
- Features/Analysis/AnalysisViewModel.swift
- Features/TaskMode/TaskModeView.swift
- Features/TaskMode/TaskModeViewModel.swift

### 页面之间的跳转流
HomeView -> ScanView -> AnalysisView -> TaskModeView -> WatchSyncView -> ResultView -> ShareCardView

### Mock 数据要求
- 提供 3 个食物分析样例：奶茶、薯片、甜甜圈
- 提供 2 个 demo task：一个 in_progress，一个 completed
- MockWorkoutService 支持进度自动增长，便于演示

### UI 风格要求
- 圆角较大
- 卡片感明显
- 主色偏黑白高级感
- 文案保留“磨平 / 结清 / 接一单 / 这口值你动多久”这套语言

### 代码输出要求
- 先给完整目录树
- 再给第一批 Swift 文件代码
- 保证代码尽量能直接放进 Xcode 编译
- 对暂未实现的 live service 用协议 + TODO 占位

请先完成第一批文件，并确保从 HomeView 到 TaskModeView 的 mock 流程可以跑通。

---

# 24. 当前结论

现在已经不只是产品执行包，而是一份更适合工程开工和 Codex 接手的开发规格。

你现在可以把这份文档继续丢给 Codex，让它先做：
- 工程目录
- SwiftUI 页面骨架
- Mock service
- Home → Scan → Analysis → TaskMode 的第一段可运行流程

只要这第一段出来，后面再接 Watch 和 HealthKit 会顺得多。

