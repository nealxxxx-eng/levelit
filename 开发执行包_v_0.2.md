# 磨平 App 开发执行包 v0.2

> 本版本基于 v0.1 + CEO/Founder-Mode Plan Review + Eng Review 的全部决策整合而成。
> CEO Review 日期: 2026-03-18
> Eng Review 日期: 2026-03-18

### Eng Review 技术修正 (9 项)

| # | 修正项 | 原方案 | 修正后 |
|---|--------|--------|--------|
| 1 | Complication 框架 | ClockKit | **WidgetKit** (watchOS 10 已弃用 ClockKit) |
| 2 | 数据同步主通道 | SwiftData + CloudKit | **WatchConnectivity 三层同步** + 本地 SwiftData |
| 3 | @Model 时机 | Phase 2 迁移 | **Phase 1 直接使用 @Model** |
| 4 | 图片存储 | `foodImageData: Data?` (blob) | **文件系统** `Documents/FoodImages/{id}.heic` |
| 5 | WC 同步策略 | 单层 sendMessage | **三层**: transferUserInfo + applicationContext + sendMessage |
| 6 | WorkoutProgress | 独立 @Model | **合并到 DebtTask** (消除 DRY 违规) |
| 7 | Package @Model | 待确认 | **Package 中定义 @Model, 各端配置 ModelContainer** |
| 8 | 测试文件 | 4 个 | **9 个** (补充 WC/Haptic/文案/超额/食物库) |
| 9 | WC 消息去重 | 无 | **时间戳去重** (防旧消息覆盖新状态) |

---

## 0. 文档目的

本文档用于把当前已经确认的产品方向和原型，压缩成一份可直接进入工程开发的执行包。

适用对象：
- iOS 开发
- Apple Watch 开发
- AI/算法接入
- Claude Code / Codex 或其他代码代理继续实现

当前版本目标：
**先完成最小闭环 MVP：拍食物 / 手动选食物 -> 估算热量 -> 生成磨平任务 -> Watch 独立运动 -> 运动完成后结清 -> 生成分享卡。**

### v0.1 -> v0.2 关键变化

| 变更项 | v0.1 | v0.2 |
|--------|------|------|
| 架构方向 | iPhone-First, Watch 为从属 | **Watch-First**, Watch 可独立接单/运动/结清 |
| 数据同步 | WatchConnectivity 单向派发 | **WatchConnectivity 三层同步** + 本地 SwiftData |
| 食物输入 | 仅拍照 | 拍照 + **预置食物库** + 自定义 kcal |
| 数据模型 | FoodTask (食物专用) | **DebtTask** (泛化, 支持 DebtType 扩展) |
| 状态机 | 6 种状态 | **8 种状态** (新增 expired, settled) |
| Watch 表盘 | 无 | **Complication** (今日欠债 kcal) |
| 触觉反馈 | 无 | **Haptic 进度反馈** (25%/50%/75%/100%) |
| 任务过期 | 无 | **48h 自动过期** |
| 运动体验 | 纯数字 | **励志文案** + **结清动画** |
| 成就系统 | 仅 Streak | **连续磨平徽章** (3/7/14/30天) |
| 超额完成 | 无 | **>120% 彩蛋分享卡** |
| 后端 API | 5 个 REST 端点 | **移除**, 改用纯本地 SwiftData + WatchConnectivity |
| Session 恢复 | 未提及 | **Phase 3 必做项** |

---

# 1. 产品定义

## 1.1 产品一句话

**拍一下你要吃的东西，看看你得动多久。**

## 1.2 产品定位

不是传统卡路里记录工具，而是一个：

**把食物热量转成 Apple Watch 运动任务，并通过"磨平/结清"机制提升行动意愿的游戏化健康产品。**

核心差异化：**Watch-First** — 手表是主战场，不是手机的附属品。

## 1.3 MVP 核心价值

用户不是"记一顿饭"，而是：
1. 拍下食物 / 在 Watch 上快选食物
2. 看到这口东西大概要花多少运动来磨平
3. 选择一种还法
4. 在 Apple Watch 上开始任务（可独立于 iPhone）
5. 运动中感受 Haptic 进度反馈 + 励志文案
6. 完成后得到"已磨平 / 已结清"的反馈 + 结清动画
7. 可选择生成战绩卡分享
8. 每次抬手看表盘，看到"今日欠债"数字

## 1.4 行为设计哲学

磨平不是事后补救工具，而是行为改变系统：
- **决策点前**: 表盘 Complication 展示欠债，提供持续行为暗示
- **决策点中**: 拍照/选食物时看到"代价"，在吃之前知道要动多久
- **行动中**: Haptic + 文案，让运动过程有陪伴感和目标感
- **行动后**: 结清动画 + 分享卡 + 徽章，提供即时成就反馈

---

# 2. MVP 范围冻结

## 2.1 本期必须做

### iPhone 端
- 首页（今日概览 + 主 CTA）
- 拍照入口
- 食物分析结果页
- **手动选择入口（预置食物库 + 自定义 kcal）**
- 任务选择页
- 任务进度页（实时同步 Watch 数据）
- 任务完成结果页 + **结清动画**
- 分享卡页
- **历史任务列表**

### Apple Watch 端
- **Watch Complication（今日欠债 kcal）**
- **独立接单（预置食物库快选 + 自定义 kcal）**
- 接收 iPhone 派发的任务
- 展示当前任务卡
- 启动运动（HKWorkoutSession）
- **Haptic 进度反馈（25%/50%/75%/100%）**
- **运动中励志文案**
- 同步运动进度
- 回传进度/完成状态
- 展示"已结清"状态

### 基础能力
- 食物热量估算（预置食物库 + mock AI）
- 热量 -> 运动时长换算
- **DebtTask 数据模型（泛化，含 DebtType）**
- **扩展状态机（8 种状态，含 expired / settled）**
- **SwiftData (本地) + WatchConnectivity 三层同步**
- HealthKit / Workout 数据读取
- **任务过期机制（48h 自动 expired）**
- 分享卡生成
- **成就徽章系统（3/7/14/30 天连续磨平）**
- **超额完成彩蛋（>120% 特殊分享卡）**

## 2.2 本期不做
- 社区系统完整实现
- 评论/好友/关注关系
- 复杂排行榜
- 多食物合并任务系统
- 多任务嵌套拆单
- 深度营养报告
- 三餐全覆盖精细识别
- 临床健康建议
- 复杂会员体系
- 真实 AI 食物识别（使用 mock + 预置库）
- 自建后端服务
- Widget / Live Activity
- Siri Shortcuts

---

# 3. 架构设计

## 3.1 Watch-First 架构

```
  iPhone (管理/拍照)               Watch (主战场)
  +------------------+            +------------------+
  | 拍照入口          |            | * 独立接单        |
  | AI 食物分析       |            | * 独立 Workout    |
  | 手动选择入口      |<---WC---->| * 独立结清        |
  | 详细统计/历史     |            | * Complication    |
  | 分享卡生成       |            | 预置食物快选       |
  | 成就徽章展示      |            | Haptic反馈        |
  | 设置/个性化      |            | 励志文案          |
  +------------------+            +------------------+
          |                              |
          v                              v
  +----------------------------------------------+
  |    Local SwiftData (各端独立持久化)            |
  |    DebtTask, UserStats, Achievement           |
  +----------------------------------------------+
          |                              |
          +---------- WC 三层同步 --------+
          | L1: transferUserInfo (任务/状态) |
          | L2: applicationContext (进度)    |
          | L3: sendMessage (实时刷新)       |
          +--------------------------------+
```

### 核心原则
- **Watch 可独立运行**: 无需 iPhone 在旁，Watch 可从预置库接单、运动、结清
- **iPhone 是管理界面**: 拍照（Watch 无相机）、详细统计、分享卡、设置
- **WatchConnectivity 三层同步**: 各端本地 SwiftData 持久化, WC 负责跨端数据传输
- **无 CloudKit 依赖**: 避免 watchOS 上 CloudKit 的不稳定和后台限制

## 3.2 数据同步策略 — WatchConnectivity 三层同步

```
  ┌─────────────────────────────────────────────────────┐
  │              WatchConnectivity 三层同步               │
  ├─────────────┬───────────────────┬───────────────────┤
  │   Layer 1   │     Layer 2       │     Layer 3       │
  │ transferUI  │ applicationContext│  sendMessage      │
  ├─────────────┼───────────────────┼───────────────────┤
  │ 任务创建     │ 运动进度回传       │ 实时 UI 刷新      │
  │ 状态变更     │ (latest-value)    │ (需 reachable)    │
  │ (可靠队列)   │                   │                   │
  ├─────────────┼───────────────────┼───────────────────┤
  │ 保证送达     │ 只保留最新值       │ 可能失败，降级L1/L2│
  │ FIFO 排队    │ 无排队，覆盖式     │ 即时送达          │
  └─────────────┴───────────────────┴───────────────────┘
```

### 同步细节

**Layer 1: transferUserInfo() — 可靠队列**
- 用途: 任务创建、状态变更（created/completed/expired/cancelled）
- 特点: FIFO 队列，系统保证后台送达，不丢失
- 消息格式: `["type": "task_sync", "task": DebtTask.toDict(), "timestamp": Date]`
- 去重策略: 接收端按 task.id + timestamp 去重，忽略旧消息

**Layer 2: updateApplicationContext() — 最新值**
- 用途: 运动进度回传（burnedCalories, progressPercent, durationSeconds）
- 特点: 只保留最新值，无队列，适合高频更新
- 更新频率: 每 5 秒推送一次（Watch → iPhone）
- 消息格式: `["type": "progress", "taskId": String, "burned": Int, "percent": Int, "duration": Int]`

**Layer 3: sendMessage() — 实时刷新**
- 用途: 双端在前台时的即时 UI 刷新触发
- 特点: 需要对端 reachable，失败时静默降级到 L1/L2
- 典型场景: iPhone 创建任务后立即通知 Watch 刷新列表

### WC 不可达降级策略
```
  iPhone 创建任务:
  1. 本地 SwiftData 保存
  2. 尝试 sendMessage() (L3)
  3. 失败 → transferUserInfo() (L1, 后台排队)
  4. Watch 重新可达时自动收到

  Watch 运动进度:
  1. 本地 SwiftData 更新
  2. 每 5 秒 updateApplicationContext() (L2)
  3. iPhone 打开 App 时读取最新 context
```

### 消息去重 (时间戳机制)
- 每条 WC 消息携带 `timestamp: Date()`
- 接收端维护 `lastProcessedTimestamp[taskId]`
- 如果收到的 timestamp <= lastProcessed, 忽略该消息
- 防止 transferUserInfo 队列中的旧消息覆盖新状态

## 3.3 Shared Swift Package

iPhone 和 Watch 共享的代码通过 Swift Package 管理：

```
  LevelItShared/
  |-- Models/
  |   |-- DebtTask.swift           (核心任务模型, 含运动进度字段)
  |   |-- PresetFood.swift         (预置食物, 纯 struct)
  |   |-- Achievement.swift        (成就徽章)
  |   |-- UserStats.swift          (用户统计)
  |   |-- DebtType.swift           (债务类型枚举)
  |   |-- TaskLevel.swift          (任务等级枚举)
  |   |-- TaskMode.swift           (任务模式枚举)
  |   |-- TaskStatus.swift         (任务状态枚举)
  |   +-- TaskSource.swift         (来源枚举)
  |
  |-- StateMachine/
  |   |-- TaskStateMachine.swift   (状态机)
  |   +-- CalorieCalculator.swift  (热量换算)
  |
  |-- Data/
  |   |-- PresetFoodLibrary.swift  (预置食物库)
  |   +-- MotivationalQuotes.swift (励志文案库)
  |
  +-- Constants/
      +-- AppConstants.swift       (全局常量)
```

### @Model 在 Package 中的配置要点
- Package.swift 需声明 `platforms: [.iOS(.v17), .watchOS(.v10)]`
- @Model 宏需要 `import SwiftData` — Package 依赖声明中无需额外 SPM 包 (SwiftData 是系统框架)
- ModelContainer 由各端 App 配置, Package 中只定义 @Model 类
- Xcode 15+ / Swift 5.9+ 完全支持此模式

---

# 4. 用户路径

## 4.1 iPhone 主路径

1. 用户打开 iPhone App 首页
2. 看到"今日欠债 xxx kcal"
3. 点击"拍一下，接一单" 或 "手动选一个"
4. **拍照路径**: 拍摄食物照片 -> App 返回分析结果
5. **手动路径**: 从预置食物库选择 / 输入自定义 kcal
6. 看到: 食物名称、热量估算、任务等级、推荐运动时长
7. 选择任务模式: 轻松磨平 / 标准磨平 / 高效磨平
8. 任务自动同步到 Apple Watch（WatchConnectivity transferUserInfo）
9. 可选: 在 iPhone 查看实时进度
10. 任务完成后看到结清动画
11. 生成分享卡

## 4.2 Watch 独立路径

1. 用户抬手看表盘，Complication 显示"今日欠债 280 kcal"
2. 点击 Complication 进入 App
3. 选择: 查看待磨平任务 / 快速接单
4. **快速接单**: 从预置食物快选（奶茶/薯片/咖啡...）或输入自定义 kcal
5. 选择任务模式
6. 直接开始运动（不需要 iPhone 参与）
7. 运动中: Haptic 反馈 + 励志文案 + 实时进度
8. 完成后: "已结清" + 同步回 iPhone

## 4.3 次路径

### 未完成任务
- 用户运动中断
- 任务保留在"待磨平"列表
- 下次可继续完成

### 部分完成
- 例如只完成 42%
- UI 显示"已磨平 42%"
- 不强制一次结清

### 任务过期
- 创建超过 48 小时未开始的任务
- 自动变为 expired 状态
- 不计入"今日欠债"
- 保留历史记录

### 超额完成
- 运动消耗超过目标 120%
- 解锁特殊分享卡: "还多了 20%, 下次可以白嗝一口"

---

# 5. 页面清单

## 5.1 iPhone 页面

### P1 首页 (HomeView)
目的: 进入主动作，展示今日状态。

模块:
- 今日欠债 kcal（醒目大字）
- 今日已结清单数
- 连续磨平天数 + 当前徽章
- 主 CTA: "拍一下，接一单"
- 副 CTA: "手动选一个"
- 最近任务列表（快速入口）

核心按钮:
- 拍照
- 手动选择
- 查看今日账单

### P2 拍照页 (ScanView)
目的: 采集食物图片。

模块:
- 相机预览
- 拍照按钮
- 取消按钮

首版要求:
- 只支持单张图
- 暂不做复杂拍照引导
- 按钮防抖（防止双击）

### P2b 手动选择页 (ManualSelectView)
目的: 不拍照时的食物输入入口。也是 AI 识别失败的降级路径。

模块:
- 预置食物分类列表（奶茶/零食/快餐/饮料/甜品）
- 搜索/筛选
- 自定义 kcal 输入
- 最近选过的食物（快捷入口）

### P3 食物分析页 (AnalysisView)
目的: 告诉用户"这口值你动多久"。

展示字段:
- 图片（拍照路径）/ 食物图标（手动路径）
- 食物名称
- 热量估算 kcal
- 任务等级（红/橙/黄/绿）
- 风险标签
- 推荐运动时长
- 文案提示

按钮:
- 生成磨平任务

### P4 任务选择页 (TaskModeView)
目的: 把同一热量转成不同"还法"。

任务模式:
- 轻松磨平: 快走
- 标准磨平: 跑走结合
- 高效磨平: 跑步/跳绳

展示字段:
- 模式名
- 描述
- 预计时长
- 是否支持分段完成

按钮:
- 创建任务（自动同步到 Watch）

### P5 任务进度页 (TaskProgressView)
目的: 显示任务状态和实时运动进度。

展示字段:
- 当前任务名称
- 当前目标 kcal
- 当前已消耗 kcal
- 当前完成进度百分比
- 当前同步状态
- 剩余 kcal

按钮:
- 返回首页

### P6 完成结果页 (ResultView)
目的: 给用户即时成就反馈。

状态:
- 进行中
- 已磨平 xx%
- 已结清
- **超额完成**（>120%）

展示字段:
- **结清动画**（食物卡被磨平消失的动效）
- 已完成百分比
- 已消耗 kcal
- 剩余 kcal
- 本单状态
- 新解锁的徽章（如有）

按钮:
- 生成分享卡
- 返回首页

### P7 分享卡页 (ShareCardView)
目的: 生成可传播战绩图。

字段:
- 食物名称
- 今日结清数
- 本单状态
- 连续磨平天数
- 当前徽章
- 一句文案
- **超额完成标记**（如适用）

按钮:
- 分享（系统分享面板）
- 保存图片

### P8 历史任务列表页 (TaskListView)
目的: 查看所有历史任务。

展示:
- 按日期分组的任务列表
- 每个任务: 食物名、kcal、状态、完成百分比
- 筛选: 全部 / 已结清 / 进行中 / 已过期

---

## 5.2 Apple Watch 页面

### W1 任务收件箱 (WatchTaskInboxView)
展示字段:
- 待磨平任务列表
- 每个任务: 食物名称、目标 kcal、推荐方式

按钮:
- 开始（进入选模式 -> 运动）
- **快速接单**（进入预置食物选择）

### W1b 快速接单页 (WatchQuickAddView)
展示字段:
- 常见食物快选网格（带 emoji）
  - 奶茶 340kcal
  - 咖啡(拿铁) 180kcal
  - 薯片 280kcal
  - 可乐 140kcal
  - 甜甜圈 260kcal
  - ...
- 自定义 kcal 输入（Digital Crown 调节）

按钮:
- 选择后直接进入任务模式选择

### W2 运动中页 (WatchWorkoutView)
展示字段:
- 当前任务名
- 已磨平百分比（大字 + 进度环）
- 已消耗 kcal
- 剩余 kcal
- 剩余分钟数（估算）
- **励志文案**（每 5 分钟轮换）

触觉反馈:
- 25% 完成: 轻触 (.notification)
- 50% 完成: 双击 (.directionUp)
- 75% 完成: 连续轻触 (.success)
- 100% 完成: 胜利序列 (.success + .success + .notification)

按钮:
- 暂停
- 结束

### W3 完成页 (WatchCompletionView)
展示字段:
- 已结清 / 超额完成
- 本次完成 kcal
- 完成时间
- 新徽章（如有）

按钮:
- 完成（返回表盘）

### W4 Complication (WidgetKit)
类型: accessoryCircular / accessoryCorner / accessoryRectangular (WidgetKit WidgetFamily)

显示:
- 今日欠债总 kcal
- 颜色: 绿(<200) / 黄(200-400) / 橙(400-600) / 红(>600)
- 点击进入 App

---

# 6. 数据模型

## 6.1 DebtTask（核心模型，替代原 FoodTask）

```swift
import SwiftData

@Model
final class DebtTask {
    @Attribute(.unique) var id: String
    var debtType: DebtType       // .food (MVP), 未来: .alcohol, .sedentary, .sleep
    var foodName: String
    var foodImageFileName: String?  // 文件系统引用: Documents/FoodImages/{id}.heic
    var estimatedCalories: Int
    var taskLevel: TaskLevel     // .green, .yellow, .orange, .red
    var taskMode: TaskMode       // .walk, .mix, .run
    var targetBurnCalories: Int
    var estimatedMinutes: Int
    var status: TaskStatus       // 8 种状态
    var progressPercent: Int
    var burnedCalories: Int
    var durationSeconds: Int     // 运动时长 (原 WorkoutProgress 字段, 合并)
    var lastSyncAt: Date?        // 最后同步时间 (原 WorkoutProgress 字段, 合并)
    var createdAt: Date
    var completedAt: Date?
    var expiredAt: Date?
    var source: TaskSource       // .iPhone, .watch
    var isOverAchieved: Bool     // 超额完成标记
}
```

### 图片存储策略

食物照片不存入 SwiftData (避免 Data blob 拖慢查询和 WC 传输):
- 存储路径: `Documents/FoodImages/{taskId}.heic`
- 写入时机: iPhone 拍照成功后
- 读取时机: 仅 iPhone 端 AnalysisView / ResultView / ShareCardView 读取
- Watch 端: 不加载图片, 只显示食物 emoji + 名称
- WC 传输: 不传图片, 只传 foodImageFileName 字段 (String?)
- 清理: 任务删除时删除对应图片文件

## 6.2 支撑枚举

```swift
enum DebtType: String, Codable {
    case food
    // 未来扩展:
    // case alcohol
    // case sedentary
    // case sleep
}

enum TaskLevel: String, Codable {
    case green   // < 120 kcal
    case yellow  // 120 - 219 kcal
    case orange  // 220 - 349 kcal
    case red     // >= 350 kcal
}

enum TaskMode: String, Codable {
    case walk  // 快走
    case mix   // 跑走结合
    case run   // 跑步
}

enum TaskStatus: String, Codable {
    case created     // 已创建
    case synced      // 已同步到 Watch (iPhone创建的任务)
    case inProgress  // 运动中
    case paused      // 暂停
    case completed   // 运动完成 (burnedCal >= targetCal)
    case settled     // 已确认结清 (用户看到结果页)
    case cancelled   // 已取消
    case expired     // 48h 未开始, 自动过期
}

enum TaskSource: String, Codable {
    case iPhone
    case watch
}
```

## 6.3 WorkoutProgress (已合并)

> **Eng Review 决定**: WorkoutProgress 的字段 (durationSeconds, lastSyncAt) 已合并到 DebtTask 中,
> 避免 DRY 违规 (burnedCalories 在两个 @Model 重复)。不再作为独立 @Model 存在。

## 6.4 PresetFood

```swift
struct PresetFood: Identifiable, Codable {
    let id: String
    let name: String
    let emoji: String
    let category: FoodCategory
    let estimatedCalories: Int
    let tags: [String]
}

enum FoodCategory: String, Codable, CaseIterable {
    case milkTea    = "奶茶"
    case snack      = "零食"
    case fastFood   = "快餐"
    case drink      = "饮料"
    case dessert    = "甜品"
    case coffee     = "咖啡"
}
```

## 6.5 Achievement

```swift
@Model
final class Achievement {
    @Attribute(.unique) var id: String
    var type: AchievementType
    var unlockedAt: Date
    var streakDays: Int
}

enum AchievementType: String, Codable {
    case streak3   = "三天新手"
    case streak7   = "七天战士"
    case streak14  = "半月达人"
    case streak30  = "月度传奇"
}
```

## 6.6 UserStats

```swift
@Model
final class UserStats {
    @Attribute(.unique) var id: String  // 固定 "current"
    var currentStreakDays: Int
    var longestStreakDays: Int
    var totalTasksCreated: Int
    var totalTasksCompleted: Int
    var totalCaloriesBurned: Int
    var lastCompletionDate: Date?
}
```

---

# 7. 状态机定义

## 7.1 任务状态（8 种）

```
  +----------+
  | created  |----------------------------------+
  +----+-----+                                  |
       | (sync to Watch, iPhone创建的任务)       | cancel
       v                                        v
  +----------+                            +----------+
  | synced   |--------------------------->| cancelled|
  +----+-----+                            +----------+
       | (start workout)                        ^
       v                                        |
  +--------------+                              |
  | in_progress  |<-----------+  cancel         |
  +----+----+----+            |                 |
       |    | pause           |                 |
       |    v                 |                 |
       |  +----------+       |                 |
       |  | paused   |-------+-----------------+
       |  +----------+  resume
       |
       | (burnedCal >= targetCal)
       v
  +-----------+
  | completed |
  +-----+-----+
        | (用户看到结果页)
        v
  +-----------+
  | settled   |
  +-----------+

  特殊转换:
  created -> expired    (48h 未开始)
  synced  -> expired    (48h 未开始)
```

注意:
- Watch 独立创建的任务跳过 synced, 直接 created -> in_progress
- expired 是终态, 不可恢复
- settled 是终态, 表示用户已确认结清

## 7.2 结清规则

当 `burnedCalories >= targetBurnCalories` 时:
- status = completed
- progressPercent = 100 (或更高)
- 如果 burnedCalories >= targetBurnCalories * 1.2: isOverAchieved = true
- 用户看到结果页后: status = settled
- 解锁分享卡

## 7.3 部分完成规则

当 `0 < burnedCalories < targetBurnCalories` 时:
- status = in_progress 或 paused
- progressPercent = floor(burnedCalories / targetBurnCalories * 100)
- 分享卡可选解锁"已磨平 xx%"版本

## 7.4 过期规则

后台定时检查（App 启动时 + 每小时一次）:
- 查询所有 status in [created, synced] 且 createdAt < now - 48h
- 批量更新为 expired
- expired 任务不计入"今日欠债"
- 保留在历史记录中

---

# 8. 热量换算与任务生成规则

## 8.1 输入
- 食物图片（拍照路径）/ 预置食物选择 / 自定义 kcal
- 食物名称
- 热量估算 kcal

## 8.2 输出
- 任务等级
- 三种任务模式的建议分钟数

## 8.3 任务等级

| 等级 | 热量范围 | 颜色 |
|------|----------|------|
| green | < 120 kcal | 绿 |
| yellow | 120 - 219 kcal | 黄 |
| orange | 220 - 349 kcal | 橙 |
| red | >= 350 kcal | 红 |

## 8.4 换算系数（首版可配置）

| 模式 | 消耗率 | 说明 |
|------|--------|------|
| walk | 8.7 kcal/分钟 | 快走 |
| mix | 13 kcal/分钟 | 跑走结合 |
| run | 18.5 kcal/分钟 | 跑步 |

```swift
func calculateMinutes(kcal: Int, mode: TaskMode) -> Int {
    let rate: Double = switch mode {
        case .walk: 8.7
        case .mix:  13.0
        case .run:  18.5
    }
    return max(1, Int(ceil(Double(kcal) / rate)))
}
```

## 8.5 热量合理性校验

AI 返回结果或用户输入需要校验:
- 最小值: 10 kcal
- 最大值: 5000 kcal
- 超出范围时提示用户确认

---

# 9. 预置食物库

## 9.1 首期食物列表（约 30-50 种）

### 奶茶类
| 名称 | emoji | kcal |
|------|-------|------|
| 黑糖珍珠奶茶 | 🧋 | 340 |
| 抹茶拿铁 | 🍵 | 250 |
| 芋泥波波 | 🫧 | 380 |
| 柠檬茶 | 🍋 | 120 |

### 零食类
| 名称 | emoji | kcal |
|------|-------|------|
| 海盐薯片 | 🥔 | 280 |
| 巧克力 | 🍫 | 230 |
| 坚果混合 | 🥜 | 180 |
| 饼干 | 🍪 | 150 |

### 快餐类
| 名称 | emoji | kcal |
|------|-------|------|
| 炸鸡 | 🍗 | 350 |
| 薯条(中) | 🍟 | 320 |
| 汉堡 | 🍔 | 450 |
| 披萨(一片) | 🍕 | 280 |

### 饮料类
| 名称 | emoji | kcal |
|------|-------|------|
| 可乐 | 🥤 | 140 |
| 橙汁 | 🧃 | 110 |
| 啤酒 | 🍺 | 150 |

### 甜品类
| 名称 | emoji | kcal |
|------|-------|------|
| 糖霜甜甜圈 | 🍩 | 260 |
| 芝士蛋糕 | 🍰 | 310 |
| 冰淇淋 | 🍦 | 200 |
| 提拉米苏 | 🧁 | 290 |

### 咖啡类
| 名称 | emoji | kcal |
|------|-------|------|
| 拿铁 | ☕ | 180 |
| 摩卡 | ☕ | 250 |
| 美式 | ☕ | 15 |
| 星冰乐 | 🧊 | 350 |

（完整列表可在 `PresetFoodLibrary.swift` 中维护，后续持续扩充）

---

# 10. 食物识别策略

## 10.1 首版策略

双路径并行:
1. **Mock AI 路径**: 图像大模型识别食物类别 -> 映射到预置热量库
2. **手动选择路径**: 用户从预置库选择 / 输入自定义 kcal

两条路径最终都输出同样的 `FoodAnalysisResult`。

## 10.2 AI 降级策略

```
  拍照 --> AI 分析
            |
            +-- 成功 --> 展示结果 (用户可修正)
            |
            +-- 失败/超时/无法识别
                  |
                  +-- 自动跳转手动选择页
                  |   "没认出来，手动选一个？"
                  |
                  +-- 日志记录失败原因
```

## 10.3 AI 返回值校验

```swift
func validateAnalysisResult(_ result: FoodAnalysisResult) -> ValidationResult {
    // 热量范围校验
    guard (10...5000).contains(result.estimatedCalories) else {
        return .suspicious("热量值异常: \(result.estimatedCalories) kcal")
    }
    // 食物名称非空
    guard !result.foodName.isEmpty else {
        return .invalid("食物名称为空")
    }
    return .valid
}
```

---

# 11. 励志文案系统

## 11.1 文案规则

- 运动中每 5 分钟在 Watch 上展示一条
- 支持进度变量替换: {{progress}}, {{remaining}}, {{food}}
- 预置 50 条, 随机不重复轮播
- 语气: 幽默、鼓励、带一点"调侃食物"的味道

## 11.2 文案示例

### 开始阶段 (0-25%)
- "出发了，{{food}}的账慢慢还"
- "第一步最难，但你已经迈出来了"
- "这口{{food}}开始紧张了"

### 中间阶段 (25-75%)
- "已经{{progress}}%了，比大多数人强"
- "还剩{{remaining}} kcal，不算多了"
- "{{food}}正在被你慢慢磨平"
- "这个速度，{{food}}扛不住了"
- "再坚持一会，你比昨天的自己强"

### 冲刺阶段 (75-99%)
- "最后冲刺！{{food}}快被磨平了"
- "还剩一点点，别在这里放弃"
- "就差{{remaining}} kcal，结清就在眼前"

### 完成时 (100%)
- "结清！这口没有赖账"
- "磨平了！你是自律的人"
- "干得漂亮，{{food}}的账清了"

---

# 12. 成就徽章系统

## 12.1 徽章类型

| 徽章 | 条件 | 图标建议 |
|------|------|----------|
| 三天新手 | 连续 3 天每天至少结清 1 单 | 铜星 |
| 七天战士 | 连续 7 天 | 银星 |
| 半月达人 | 连续 14 天 | 金星 |
| 月度传奇 | 连续 30 天 | 钻石 |

## 12.2 Streak 计算规则

```swift
func calculateStreak(stats: UserStats, today: Date) -> Int {
    guard let lastDate = stats.lastCompletionDate else { return 0 }

    let calendar = Calendar.current
    let daysSinceLast = calendar.dateComponents([.day],
        from: calendar.startOfDay(for: lastDate),
        to: calendar.startOfDay(for: today)).day ?? 0

    switch daysSinceLast {
    case 0: return stats.currentStreakDays      // 今天已完成
    case 1: return stats.currentStreakDays      // 昨天完成, 今天还有机会
    default: return 0                            // 断了
    }
}
```

注意: 使用用户本地时区计算"一天"的边界。

## 12.3 徽章展示位置
- 首页: 当前徽章 icon
- 分享卡: 徽章图标 + 连续天数
- Watch: 结清页显示新获得的徽章

---

# 13. 分享卡规则

## 13.1 分享卡类型

### A. 已结清卡
文案示例:
- 已结清: 黑糖珍珠奶茶
- 这口我没有赖账
- 今日结清 1 单
- [徽章图标] 连续磨平 7 天

### B. 部分完成卡
文案示例:
- 已磨平 42%
- 先还一半也算赢
- 今天先把它拉回来一点

### C. 超额完成卡 (新增)
文案示例:
- 超额完成 127%!
- 这口奶茶还多了 27%, 下次可以白嗝一口
- 今日结清 1 单 (超额!)

## 13.2 生成方式
使用前端本地 SwiftUI 渲染导出图片。
首版无需服务端生成。

## 13.3 分享前处理
- 自动剥离 EXIF 信息（GPS、设备信息等）
- 相册保存权限被拒时降级为"复制到剪贴板"

---

# 14. 结清动画

## 14.1 动画描述

任务结清时, 在结果页展示 1-2 秒的"磨平"动画:
1. 食物卡片从正常状态开始
2. 逐渐被"压扁"（scaleEffect Y 轴从 1.0 到 0.1）
3. 同时透明度降低（opacity 从 1.0 到 0.0）
4. 最后消失, 替换为"已结清"大字 + 纸屑效果

## 14.2 技术实现

```swift
// 概念代码
withAnimation(.spring(duration: 0.8)) {
    foodCardScale = 0.1
    foodCardOpacity = 0.0
}
// 延迟后显示结清状态
DispatchQueue.main.asyncAfter(deadline: .now() + 1.0) {
    withAnimation(.spring(duration: 0.5)) {
        showSettledState = true
    }
}
```

使用 SwiftUI 原生动画, 不引入第三方库。

---

# 15. Haptic 反馈设计

## 15.1 进度节点反馈

| 进度 | Haptic 类型 | watchOS API |
|------|------------|-------------|
| 25% | 轻触提醒 | WKInterfaceDevice.current().play(.notification) |
| 50% | 方向感反馈 | WKInterfaceDevice.current().play(.directionUp) |
| 75% | 成功提示 | WKInterfaceDevice.current().play(.success) |
| 100% | 胜利序列 | .success + delay + .success + delay + .notification |

## 15.2 其他触觉反馈
- 任务创建成功: .click
- 任务过期提示: .failure
- 选择食物: .click

---

# 16. 错误处理策略

## 16.1 总原则
- 零静默失败: 每个错误必须有用户可见的反馈
- 优雅降级: 核心流程不因单点故障中断
- 日志记录: 所有错误记录到 os_log

## 16.2 关键错误处理

| 错误场景 | 处理策略 | 用户看到 |
|----------|----------|----------|
| 相机权限被拒 | 引导去系统设置 | "需要相机权限才能拍照" |
| AI 分析超时 | 重试2次, 降级到手动选择 | "识别超时，手动选一个？" |
| AI 返回无法识别 | 直接跳转手动选择 | "没认出来，手动选一个？" |
| AI 返回异常热量 | 弹窗确认 | "识别结果: 8500 kcal, 确认？" |
| HealthKit 权限被拒 | 降级为手动计时模式 | "未授权运动数据，使用手动计时" |
| Workout Session 已存在 | 提示先结束 | "你还有一个运动在进行中" |
| HealthKit 0 消耗 | 时间估算替代 + 佩戴提示 | "检测异常，请确认佩戴正确" |
| Watch 未配对 | 提示配对(仅iPhone->Watch流程) | "请先配对 Apple Watch" |
| WC 不可达 | 本地 SwiftData 先行, transferUserInfo 排队 | 透明（用户无感） |
| 分享卡渲染失败 | 降低分辨率重试 | "正在生成..." |
| 相册保存权限被拒 | 复制到剪贴板 | "已复制到剪贴板" |

## 16.3 Session Recovery（Phase 3 必做）

```
  App 被系统杀掉时:
  1. HKWorkoutSession 可能仍在后台运行
  2. App 重新启动时:
     a. 检查是否有活跃的 HKWorkoutSession
     b. 如果有, 恢复关联的 DebtTask
     c. 显示 "上次运动被中断, 是否继续?"
     d. 用户选择继续: 恢复 workout session
     e. 用户选择放弃: 保存已完成部分, 标记为 paused

  App 被强制退出时:
  1. workout session 被终止
  2. 已消耗的卡路里从 HealthKit 查询恢复
  3. 任务更新为部分完成状态
```

---

# 17. 隐私与安全

## 17.1 App Store 审核必需

- Info.plist 中必须包含:
  - `NSCameraUsageDescription`: "用于拍摄食物照片，估算热量"
  - `NSHealthShareUsageDescription`: "用于读取运动消耗数据，计算磨平进度"
  - `NSHealthUpdateUsageDescription`: "用于记录运动数据"
  - `NSPhotoLibraryAddUsageDescription`: "用于保存分享卡到相册"
- 隐私政策页面（App Store Connect 必填链接）

## 17.2 数据处理原则

- HealthKit 数据仅在设备本地使用, 不上传到任何服务器
- 所有数据仅存储在用户设备本地 (SwiftData), 通过 WatchConnectivity 在配对设备间同步
- 食物照片仅存储在 iPhone 本地文件系统 (Documents/FoodImages/), 不传输到 Watch
- 分享卡导出前自动剥离 EXIF 元数据
- 不收集用户画像、不做广告追踪

## 17.3 文案注意事项

避免使用:
- "减肥" "瘦身" "健康建议" "医疗" 等措辞
- 不做任何健康声明或医疗建议

可以使用:
- "磨平" "结清" "运动" "热量估算" 等中性词汇

---

# 18. 埋点设计

## 18.1 核心埋点

| 事件 | 触发时机 | 参数 |
|------|----------|------|
| app_open | App 启动 | source: iPhone/watch |
| home_view | 首页曝光 | pending_kcal, streak_days |
| tap_scan | 点击拍照 | - |
| scan_complete | 拍照完成 | - |
| tap_manual_select | 点击手动选择 | - |
| food_analysis_success | AI 识别成功 | food_name, kcal, latency_ms |
| food_analysis_fail | AI 识别失败 | error_type |
| food_manual_selected | 手动选择食物 | food_name, kcal, is_preset |
| task_created | 任务创建 | food_name, kcal, mode, source |
| watch_task_start | Watch 任务开始 | task_id, mode |
| watch_progress_25 | 进度 25% | task_id |
| watch_progress_50 | 进度 50% | task_id |
| watch_progress_75 | 进度 75% | task_id |
| task_completed | 任务完成 | task_id, duration_seconds, is_over_achieved |
| task_expired | 任务过期 | task_id, age_hours |
| share_card_generated | 分享卡生成 | card_type, food_name |
| share_tap | 点击分享 | platform |
| achievement_unlocked | 徽章解锁 | achievement_type, streak_days |
| complication_tap | 表盘点击进入 | pending_kcal |
| healthkit_auth_granted | HealthKit 授权 | - |
| healthkit_auth_denied | HealthKit 被拒 | - |
| session_recovered | 运动恢复 | task_id |

## 18.2 关键运营指标

- **日活 (DAU)**: app_open 去重
- **拍照转化率**: scan_complete / tap_scan
- **任务完成率**: task_completed / task_created
- **Watch 独立使用率**: task_created(source=watch) / task_created(total)
- **AI 识别成功率**: food_analysis_success / (success + fail)
- **手动降级率**: food_manual_selected(after_fail) / food_analysis_fail
- **分享率**: share_tap / task_completed
- **留存钩子**: streak_days 分布

## 18.3 实现方案

MVP 阶段使用 `os_log` + 本地 JSON 日志。
后续可无缝接入 Firebase Analytics 或 PostHog。

---

# 19. iOS 技术栈

## 19.1 iPhone 技术栈
- SwiftUI
- SwiftData (本地持久化, 无 CloudKit)
- MVVM (Observable)
- Async/Await
- HealthKit
- WatchConnectivity (三层同步主通道)
- AVFoundation (相机)

## 19.2 Apple Watch 技术栈
- watchOS 10+ (SwiftData 要求)
- SwiftUI
- SwiftData (本地持久化, 无 CloudKit)
- HealthKit
- HKWorkoutSession
- HKLiveWorkoutBuilder
- WatchConnectivity (三层同步)
- **WidgetKit** (Complication — watchOS 10+ 已弃用 ClockKit)

## 19.3 状态同步机制

```
  ┌─────────────────────────────────────────────────┐
  │                 数据架构                          │
  ├────────────────────┬────────────────────────────┤
  │   iPhone           │        Watch               │
  │  SwiftData (本地)   │   SwiftData (本地)          │
  └────────┬───────────┴──────────┬─────────────────┘
           │                      │
           └──── WatchConnectivity ────┘
                 三层同步:
                 L1: transferUserInfo()      任务/状态 (可靠队列)
                 L2: updateApplicationContext() 运动进度 (最新值)
                 L3: sendMessage()           实时刷新 (可选)
```

- **WatchConnectivity 是数据同步主通道**: 替代 CloudKit (在 watchOS 上不稳定)
- **各端本地 SwiftData 独立持久化**: 断连时本地数据完整可用
- **三层策略互补**: L1 可靠但延迟, L2 高频但只保最新, L3 实时但不保证
- **时间戳去重**: 每条消息带 timestamp, 接收端忽略旧消息

---

# 20. 性能约束

## 20.1 Watch 端内存预算

Apple Watch 内存极为有限, 必须严格控制:

| 模块 | 预算 |
|------|------|
| App 基础开销 | ~30 MB |
| SwiftUI 渲染 | ~20 MB |
| SwiftData | ~10 MB |
| HealthKit Session | ~15 MB |
| 预留给系统 | ~50 MB |
| 安全余量 | ~30 MB |
| **总计上限** | **~155 MB (of ~256 MB)** |

**核心限制: Watch 端不加载食物图片。** 任务卡只显示食物名称 + emoji + 数字。

## 20.2 其他性能要点

- SwiftData 查询: 为 `createdAt` 字段加索引
- HealthKit 数据: 使用 delegate 推送, 不主动轮询
- 分享卡渲染: 后台线程, 最大分辨率 1080p
- 食物照片: 限制最大 1MB, HEIC 压缩
- UI 更新节流: Watch 运动中页面刷新不超过 1次/秒

---

# 21. 工程文件结构

## 21.1 Xcode 项目结构

```
LevelIt/                          (Xcode Project)
|
|-- LevelItShared/                (Swift Package - 共享代码)
|   |-- Package.swift             (platforms: .iOS(.v17), .watchOS(.v10))
|   +-- Sources/
|       |-- Models/
|       |   |-- DebtTask.swift    (@Model, 含运动进度字段)
|       |   |-- PresetFood.swift  (纯 struct, 非 @Model)
|       |   |-- Achievement.swift (@Model)
|       |   |-- UserStats.swift   (@Model)
|       |   |-- DebtType.swift
|       |   |-- TaskLevel.swift
|       |   |-- TaskMode.swift
|       |   |-- TaskStatus.swift
|       |   +-- TaskSource.swift
|       |-- StateMachine/
|       |   |-- TaskStateMachine.swift
|       |   +-- CalorieCalculator.swift
|       |-- Data/
|       |   |-- PresetFoodLibrary.swift
|       |   +-- MotivationalQuotes.swift
|       +-- Constants/
|           +-- AppConstants.swift
|
|-- LevelIt/                      (iPhone App)
|   |-- App/
|   |   |-- LevelItApp.swift
|   |   +-- AppCoordinator.swift
|   |
|   |-- Core/
|   |   |-- DesignSystem/
|   |   |   |-- Colors+.swift
|   |   |   |-- Typography+.swift
|   |   |   +-- Components/
|   |   |       |-- PrimaryButton.swift
|   |   |       |-- TaskLevelBadge.swift
|   |   |       |-- StatCard.swift
|   |   |       |-- ProgressRing.swift
|   |   |       +-- AchievementBadge.swift
|   |   +-- Extensions/
|   |       +-- Date+.swift
|   |
|   |-- Services/
|   |   |-- FoodAnalysisService.swift       (Protocol + Mock)
|   |   |-- WorkoutSyncService.swift        (WatchConnectivity)
|   |   |-- TaskExpiryService.swift         (过期检查)
|   |   |-- StreakService.swift             (Streak + 徽章)
|   |   +-- ShareCardRenderer.swift         (分享卡渲染)
|   |
|   |-- Features/
|   |   |-- Home/
|   |   |   |-- HomeView.swift
|   |   |   +-- HomeViewModel.swift
|   |   |
|   |   |-- Scan/
|   |   |   |-- ScanView.swift
|   |   |   +-- ScanViewModel.swift
|   |   |
|   |   |-- ManualSelect/
|   |   |   |-- ManualSelectView.swift
|   |   |   +-- ManualSelectViewModel.swift
|   |   |
|   |   |-- Analysis/
|   |   |   |-- AnalysisView.swift
|   |   |   +-- AnalysisViewModel.swift
|   |   |
|   |   |-- TaskMode/
|   |   |   |-- TaskModeView.swift
|   |   |   +-- TaskModeViewModel.swift
|   |   |
|   |   |-- Progress/
|   |   |   |-- TaskProgressView.swift
|   |   |   +-- TaskProgressViewModel.swift
|   |   |
|   |   |-- Result/
|   |   |   |-- ResultView.swift
|   |   |   |-- ResultViewModel.swift
|   |   |   +-- SettledAnimationView.swift
|   |   |
|   |   |-- Share/
|   |   |   |-- ShareCardView.swift
|   |   |   +-- ShareCardViewModel.swift
|   |   |
|   |   +-- TaskList/
|   |       |-- TaskListView.swift
|   |       +-- TaskListViewModel.swift
|   |
|   +-- Resources/
|       +-- Assets.xcassets
|
|-- LevelItWatch/                 (watchOS App)
|   |-- App/
|   |   |-- LevelItWatchApp.swift
|   |   +-- WatchRootView.swift
|   |
|   |-- Services/
|   |   |-- WatchWorkoutManager.swift
|   |   |-- WatchSyncReceiver.swift
|   |   |-- WatchHapticManager.swift
|   |   +-- WatchHealthPermissionManager.swift
|   |
|   |-- Features/
|   |   |-- TaskInbox/
|   |   |   |-- WatchTaskInboxView.swift
|   |   |   +-- WatchTaskInboxViewModel.swift
|   |   |-- QuickAdd/
|   |   |   |-- WatchQuickAddView.swift
|   |   |   +-- WatchQuickAddViewModel.swift
|   |   |-- Workout/
|   |   |   |-- WatchWorkoutView.swift
|   |   |   |-- WatchWorkoutViewModel.swift
|   |   |   +-- Components/
|   |   |       |-- WatchProgressRing.swift
|   |   |       +-- WatchMotivationalText.swift
|   |   +-- Completion/
|   |       |-- WatchCompletionView.swift
|   |       +-- WatchCompletionViewModel.swift
|   |
|   +-- Complication/
|       +-- DebtComplicationProvider.swift  (WidgetKit, 非 ClockKit)
|
+-- LevelItTests/                 (测试 — 9 个文件)
    |-- StateMachineTests.swift          (状态机转换)
    |-- CalorieCalculatorTests.swift     (热量换算)
    |-- StreakCalculatorTests.swift      (Streak + 时区)
    |-- TaskExpiryTests.swift            (48h 过期)
    |-- WCSyncTests.swift                (WC 三层同步 + 时间戳去重)
    |-- HapticScheduleTests.swift        (进度 → 触觉映射)
    |-- MotivationalQuotesTests.swift    (文案分段 + 不重复)
    |-- OverAchievementTests.swift       (>120% 彩蛋判定)
    +-- PresetFoodLibraryTests.swift     (食物库数据完整性)
```

---

# 22. 开发阶段拆解

## Phase 1: 工程搭建 + UI 骨架 + Mock 闭环

目标: 用 mock 数据跑通完整 UI 流程

任务:
- 创建 Xcode 项目 (iOS + watchOS targets)
- 创建 LevelItShared Swift Package
- 实现所有数据模型 (DebtTask, PresetFood, Achievement, UserStats 等, 使用 @Model)
- 实现状态机 (TaskStateMachine)
- 实现热量换算 (CalorieCalculator)
- 实现预置食物库 (PresetFoodLibrary)
- 实现励志文案库 (MotivationalQuotes)
- iPhone: 所有页面 UI (Home -> Scan -> ManualSelect -> Analysis -> TaskMode -> Progress -> Result -> Share -> TaskList)
- Watch: 所有页面 UI (TaskInbox -> QuickAdd -> Workout -> Completion)
- Watch: Complication — WidgetKit (静态 mock 数据)
- Watch: Haptic 反馈
- 结清动画
- 成就徽章系统
- 超额完成彩蛋
- Mock 运动模拟器 (定时器模拟进度增长)
- 基础单元测试 (状态机, 热量换算, Streak)

## Phase 2: SwiftData 持久化 + WatchConnectivity 同步

目标: 数据持久化 + 双端同步

任务:
- 所有 @Model 已在 Phase 1 就位 (DebtTask, Achievement, UserStats)
- WatchConnectivity 三层同步实现
  - L1: transferUserInfo (任务/状态)
  - L2: applicationContext (运动进度)
  - L3: sendMessage (实时刷新)
- 时间戳去重机制
- WC 不可达降级策略
- 任务过期后台检查服务
- WidgetKit Complication 动态数据 (TimelineProvider + WidgetKit reloadTimelines)
- Streak 计算持久化
- 断连时本地操作 + 重连后自动同步

## Phase 3: HealthKit / Watch Workout 接入

目标: 使用真实运动数据驱动任务完成

任务:
- HealthKit 权限请求
- Watch: HKWorkoutSession 创建和管理
- Watch: HKLiveWorkoutBuilder 实时数据
- activeEnergyBurned 驱动进度
- 自动结清逻辑
- **Session Recovery (必做)**
  - 活跃 session 检测
  - 中断恢复
  - 部分完成保存
- HealthKit 权限被拒降级方案
- 0 消耗异常处理

## Phase 4: 分享卡 + Polish + 上架准备

目标: 完成可发布版本

任务:
- 分享卡 SwiftUI -> UIImage 渲染
- EXIF 剥离
- 系统分享面板集成
- 相册保存 (含降级)
- 隐私政策页面
- App Store 截图
- Info.plist 权限说明文案
- 深色模式适配
- Loading 状态
- 错误态 UI
- 全流程 QA
- TestFlight 内测

---

# 23. 两周开发优先级

## 第 1 周 (Phase 1)
- Xcode 项目 + Shared Package 搭建
- 数据模型 + 状态机 + 热量换算
- 预置食物库
- iPhone 页面: Home -> Scan/ManualSelect -> Analysis -> TaskMode
- Watch 页面: TaskInbox -> QuickAdd
- Mock 数据驱动 UI

## 第 2 周 (Phase 1 续 + Phase 2 开始)
- iPhone 页面: Progress -> Result (含结清动画) -> Share -> TaskList
- Watch 页面: Workout (含 Haptic + 文案) -> Completion
- Watch Complication
- 成就徽章 + 超额完成
- Mock 运动模拟器
- WatchConnectivity 三层同步开始
- 单元测试

---

# 24. TODOS (Phase 2+)

| 优先级 | 项目 | 描述 | 预计工程量 |
|--------|------|------|-----------|
| P2 | 食物"前科记录" | 再次拍到同类食物时显示上次磨平耗时 | 4h |
| P2 | 周度"账单"报告 | 每周自动生成精美周报卡 | 6-8h |
| P2 | 智能时段提示 | 在常见进食时段发送本地通知 | 3-4h |
| P2 | 真实 AI 食物识别 | 接入大模型 API 替代 mock | 8-16h |
| P2 | 个性化热量换算 | 基于体重/性别/年龄修正系数 | 4h |
| P3 | Widget / Live Activity | 桌面小组件显示今日欠债 | 6h |
| P3 | Siri Shortcuts | "Hey Siri, 我喝了一杯奶茶" | 4h |
| P3 | 用户账户 (Sign in with Apple) | 社交功能基础 | 8h |
| P3 | 朋友对赌模式 | 两人同时接单, 谁先结清谁赢 | 16h |
| P3 | 排行榜 | 需要后端支持 | 16h+ |
| P4 | 订阅变现 | 高级 AI + 个性化 | TBD |
| P4 | 多种债务类型 | alcohol/sedentary/sleep | TBD |

---

# 25. 给 Claude Code / Codex 的任务 Prompt

## 第一轮: 工程搭建 + 共享代码

你现在要实现一个名为"磨平 (LevelIt)"的 iOS + Apple Watch MVP。

### 产品目标
用户拍摄食物照片或从预置库手动选择，系统估算热量，生成一个"磨平任务"。Apple Watch 可以独立接单和运动。用户完成运动后，通过 HealthKit 数据更新任务进度，达到目标后任务标记为 completed/settled。

### 架构要求
- **Watch-First**: Apple Watch 可独立运行（接单/运动/结清），iPhone 是管理界面
- **SwiftData 本地持久化** + **WatchConnectivity 三层同步** (Phase 2 接入, Phase 1 先用本地 SwiftData)
- **不使用 CloudKit** (watchOS 上不稳定, 用 WC 替代)
- **Shared Swift Package**: 共享 @Model 模型和业务逻辑

### 请先完成
1. 创建 Xcode 项目 (LevelIt)，包含 iOS target 和 watchOS target
2. 创建 LevelItShared Swift Package
3. 实现以下共享模型:
   - DebtTask (含 DebtType 泛化, 含运动进度字段 durationSeconds/lastSyncAt)
   - PresetFood + PresetFoodLibrary (30+ 种食物)
   - Achievement + AchievementType
   - UserStats
   - TaskStateMachine (8 种状态的合法转换)
   - CalorieCalculator (三种模式换算)
   - MotivationalQuotes (50 条)
4. 单元测试: TaskStateMachineTests, CalorieCalculatorTests

### 代码要求
- SwiftUI + MVVM (@Observable)
- Swift 5.9+, iOS 17+, watchOS 10+
- 模块化, 每个 View/ViewModel 分文件
- 中文注释

---

## 第二轮: iPhone UI

基于已建立的 LevelItShared, 实现 iPhone 端所有页面:

1. HomeView (今日概览 + CTA)
2. ScanView (相机拍照)
3. ManualSelectView (预置食物库 + 自定义 kcal)
4. AnalysisView (食物分析结果)
5. TaskModeView (选择还法)
6. TaskProgressView (任务进度)
7. ResultView (结清结果 + 结清动画)
8. ShareCardView (分享卡)
9. TaskListView (历史任务)

页面跳转流:
Home -> Scan/ManualSelect -> Analysis -> TaskMode -> Progress -> Result -> Share

使用 mock 数据驱动。Mock 运动模拟器每秒增加 8-15 kcal。

UI 风格: 圆角卡片、黑白高级感、"磨平/结清/接一单"语言体系。

---

## 第三轮: Watch UI

实现 Watch 端所有页面:

1. WatchTaskInboxView (任务列表)
2. WatchQuickAddView (预置食物快选)
3. WatchWorkoutView (运动中 + 进度环 + Haptic + 励志文案)
4. WatchCompletionView (完成)
5. DebtComplicationProvider (WidgetKit Complication, 非 ClockKit)

Watch 端必须能独立运行完整流程: QuickAdd -> Workout -> Completion

---

# 26. 当前结论

v0.2 相比 v0.1 的核心升级:
1. 从"iPhone 带 Watch"变成"Watch-First"
2. 从"纯拍照"变成"拍照 + 手动选择"双路径
3. 从"记录工具"变成"行为改变系统"（Complication + 徽章 + 文案 + 动画）
4. 从"临时数据"变成"SwiftData 本地持久化 + WatchConnectivity 三层同步"
5. 从"单一食物"变成"DebtType 泛化平台"

这份文档可以直接交给 Claude Code 或 Codex 开始实现。建议从第 25 节的第一轮 Prompt 开始。
