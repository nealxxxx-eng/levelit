# 磨平 LevelIt — App Store 上架准备清单 (v1.0)

> 目标：把当前版本提交 App Store 审核。
> 既有素材见 `AppStore资料.md`（名称/副标题/关键词/描述/隐私声明草案）。
> 本清单聚焦**还差什么**，按"硬阻断 → 必做 → 建议"排序。

---

## 🔴 硬阻断（不解决无法提交 / 大概率被拒）

### 1. 全程 HTTPS（最高优先 · 当前被大陆备案阻断，已暂缓）
- **现状**：App 通过明文 `http://39.105.196.84`（IP）传输**登录密码、JWT、食物照片、好友关系**；`Info.plist` 开了 ATS 例外。
- **风险**：Apple 5.1.1 要求敏感数据加密传输；明文传密码/健康数据是明确拒绝理由。**HTTPS 是上架硬门槛**。

- **2026-06 诊断结论（重要，已验证）**：
  - 已在服务器搭好 HTTPS：DuckDNS 子域名 `levelit.duckdns.org` + Let's Encrypt 证书（DNS-01 签发，acme.sh 自动续期）+ 8443 端口 TLS 终结器（`levelit-tls.mjs` → 转发本机 :80）。**按 IP 直连 8443 返回 200，证书与服务均正常。**
  - **但按域名连接被重置**：实测 TLS 握手 SNI=域名 → 连接 000（reset）；SNI=IP → 200。这是**阿里云大陆机房入口的 ICP 备案 SNI 拦截**——未备案域名在任意端口都被掐。拦截在服务器入口，故任何客户端（含海外审核员）按域名都连不上。
  - 结论：**当前大陆 ECS + 未备案域名，HTTPS 走不通；换端口/换证书无效。**

- **恢复 HTTPS 的两条路（择一）**：
  1. **迁香港/海外区 ECS（推荐）**：新开 HK ECS → 用现成脚本重新部署后端（`deploy-auth.sh` + 代理 + `setup-https.sh`/`finish-https.sh`）→ 迁移 `users.json`/`pk.json`/`social.json` → DuckDNS 改指向新 IP。香港无需备案，域名+已签证书立即可用。HTTPS 设施已在本仓库就绪。
  2. **ICP 备案**：保留大陆服务器，但需**自有域名**（DuckDNS 不行）走完备案（数周、需材料）。

- **HTTPS 通后客户端只需**：`APIConfig.swift` 改 `scheme="https"` / `host=域名` / `port=8443`（一处），并移除 `Info.plist` 的 ATS 例外。客户端网络层已全部走 `APIConfig`，无需逐个改。

- **当前决定**：暂缓上架，继续用 IP 明文做内部测试。**带真实用户/正式上架前必须先解决本项。**

### 2. 付费 Apple Developer 账号
- 免费个人 Team **无法**提交 App Store（也加不了推送等能力）。
- 需注册 **Apple Developer Program（$99/年）**，再在 Xcode 用该团队签名。

### 3. 应用内删除账号（Apple 5.1.1(v)）
- **现状**：仅有退出登录，**无删除账号**。支持账号注册的 App 必须提供应用内删除入口。
- **要做**：
  - 后端加 `DELETE /api/auth/account`（删用户 + 其 PK/好友数据）。
  - 设置页加"删除账号"入口（二次确认 + 不可逆提示）。

---

## 🟠 提交前必做

### 4. App Privacy（隐私营养标签）
在 App Store Connect 如实勾选收集的数据类型并关联用途：
- **联系信息**：邮箱/手机号（账号）
- **健康与健身**：身高体重、运动消耗、磨平数据
- **用户内容**：食物照片（上传 AI 分析）
- **标识符**：账号 userId、设备 token（推送）
- 关联用途：App 功能 / 账号管理；声明是否用于追踪（应为否）。

### 5. 隐私政策 URL
- 准备可公开访问的隐私政策页面（内容见 `PRIVACY.md`），填入 App Store Connect 与 App 内"关于"。
- 须覆盖：收集什么、为何收集、存储位置、保留与删除、第三方（AI 服务）。

### 6. 加密出口合规
- 在 `Info.plist` 加 `ITSAppUsesNonExemptEncryption = NO`（仅用 HTTPS/系统加密，属豁免），免去每次构建手填。

### 7. 审核用演示账号
- App 需登录才能用，**必须**在"App 审核信息"里提供可用的测试账号（已注册的邮箱+密码），并备注 PK/广场如何体验。

### 8. 权限用途文案（已具备，复核即可）
- 相机：`用于拍摄食物照片并上传 AI 分析，估算热量` ✅
- HealthKit 读：`用于读取运动消耗数据，计算磨平进度` ✅
- HealthKit 写：`用于记录运动数据` ✅
- 相册添加：`用于保存分享卡到相册` ✅
- 复核措辞是否与实际行为一致（审核会逐条核对）。

### 9. 版本与构建号
- `MARKETING_VERSION` 1.0、`CURRENT_PROJECT_VERSION` 1 —— 首次提交可用；后续每次提交递增 build。

### 10. 上架素材（见 `AppStore资料.md`）
- App 名称/副标题/关键词/描述/分类/年龄分级 —— 已有草案，复核。
- **截图**：6.7"/6.5" iPhone（必需）、Apple Watch（若提交 Watch App）。
- **App 图标**：1024×1024 无 alpha。

---

## 🟡 建议（不阻断，但影响过审顺滑度/质量）

- **#3 接口限流**：防爆破/刷量，降低被滥用风险。
- **推送能力**：付费账号后加 Push Notifications capability + 服务端 APNs 证书环境变量，PK 通知才真正可用（当前无能力时静默失败、不影响审核）。
- **崩溃/卡死**：上架前在真机多账号跑通 PK 全流程；历史真机卡死多为 SwiftData 旧库状态，已加迁移兜底，建议保留"全新安装可恢复"的预期。
- **Watch App 提交决策**：决定本次是否一并提交 Watch App（需对应截图与功能完整性）。
- **HealthKit 审核备注**：说明仅用于计算磨平进度、不用于广告、不与第三方共享。

---

## 上架执行顺序（建议）

1. 注册付费开发者账号（2 起步，有审核等待）。
2. **配 HTTPS**（1）+ 客户端改 https + 去 ATS 例外。
3. 加**删除账号**（3）后端 + UI。
4. 加密合规（6）、隐私政策上线（5）、隐私标签（4）。
5. Xcode 用付费团队 Archive → 上传 → 填元数据/截图/演示账号 → 提交审核。

> 当前可立即并行推进的：**HTTPS（我可给脚本）**、**删除账号**、**加密合规声明**、**隐私政策定稿**。付费账号注册建议尽早开始（有等待期）。
